import { StyleSheet } from 'react-native';

export default StyleSheet.create( {
    emptyListBanner: {
        textAlign: 'center',
        textAlignVertical: 'center',
        marginTop: 60,
        padding: 30,
        fontSize: 20,
        color: '#666'
    }
} );